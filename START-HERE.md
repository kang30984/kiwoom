# 시세 터미널 — 테스트 안내

**API 키 없이 바로 돌아갑니다.** 처음 실행하면 가짜 시세(데모 모드)로 시작하므로 증권사 계좌나 키 발급 없이 화면과 기능을 전부 확인할 수 있습니다.

⚠️ **데모 모드의 숫자는 실제 시세가 아닙니다.** 프로그램이 만들어낸 가짜 값입니다. 투자 판단에 쓰지 마세요.

---

## 1. Node.js 설치 (한 번만)

<https://nodejs.org> 에서 **LTS** 버전을 받아 설치하세요. **Node 20 이상**이 필요합니다.

설치 후 확인 — 명령 프롬프트(또는 터미널)에서:

```
node -v
```

`v20.x.x` 이상이 나오면 됩니다. 아무것도 안 나오면 컴퓨터를 다시 시작한 뒤 확인하세요.

## 2. 압축 풀기

ZIP을 원하는 폴더에 저장하고 **우클릭 → 압축 풀기**.

압축을 푼 폴더 안에 `server`, `web`, `start.bat` 이 보여야 정상입니다.

> 폴더가 `kiwoom-terminal\kiwoom-terminal\` 처럼 두 겹으로 생겼다면 안쪽 폴더에서 실행하세요. 실행 스크립트가 알아서 찾아 들어가기도 합니다.

## 3. 실행

| 운영체제 | 방법 |
|---|---|
| Windows | `start.bat` **더블클릭** |
| macOS / Linux | 터미널에서 폴더로 이동한 뒤 `bash start.sh` |

첫 실행은 필요한 패키지를 내려받느라 **1~3분** 걸립니다. 창이 멈춘 것처럼 보여도 기다려 주세요. **인터넷 연결이 필요합니다.**

준비가 끝나면 브라우저가 자동으로 열립니다 → <http://localhost:5173>

창이 두 개 뜹니다. **둘 다 열어둬야** 동작합니다.

- `Kiwoom Backend` — 데이터를 가져오는 쪽
- 원래 창 — 웹 화면을 띄우는 쪽

## 4. 종료

두 창을 모두 닫으면 됩니다. 안 닫히면 명령 프롬프트에서:

```
taskkill /f /im node.exe
```

---

## 무엇을 봐주시면 좋을까요

- **국내 / 미국** 탭 전환 — 미국은 상승이 초록, 국내는 상승이 빨강입니다 (각 시장 관례)
- **종목 검색** — 이름 일부만 넣어도 후보가 뜹니다 (예: `삼성`, `하이닉스`, `apple`)
- **차트** — 월봉 / 주봉 / 일봉 / 분봉
- **호가창** — 국내 탭 오른쪽. 매수·매도 잔량이 좌우로 뻗습니다
- **매매계획** — 자금과 감당할 손실을 넣고 `계산하기`. 손절가·익절가·수량이 나옵니다
- **긴 종목명** — `...` 으로 잘린 이름에 마우스를 올리면 전체가 뜹니다

## 문제가 생기면

먼저 `check.bat` (Windows)을 실행해 보세요. 설치 상태를 점검해 알려줍니다.

| 증상 | 해결 |
|---|---|
| `Node.js not found` | 1번 단계를 다시. 설치 후 창을 닫고 새로 열어야 인식됩니다 |
| 화면이 열리는데 숫자가 전부 `—` | 백엔드 창이 닫혔는지 확인. `start.bat` 재실행 |
| `EADDRINUSE` | 4000 또는 5173 포트를 다른 프로그램이 씀. `server\.env` 의 `PORT` 를 4001 등으로 변경 |
| `npm install` 실패 | 인터넷 연결 또는 회사 프록시 문제. 다른 네트워크에서 시도 |
| Windows 보안 경고 | 인터넷에서 받은 `.bat` 파일이라 뜹니다. `추가 정보` → `실행` |
| 파일을 고쳤는데 반영 안 됨 | 백엔드 창을 닫고 `start.bat` 재실행 + 브라우저 `Ctrl+Shift+R` |

화면 왼쪽 위에 `ver ...` 이 표시됩니다. 문제를 알려주실 때 이 값을 함께 적어주시면 도움이 됩니다.

---

## 실제 시세로 보려면 (선택)

데모가 아닌 실제 시세를 보려면 **본인 명의의 키가 필요합니다.**

- **국내** — 키움증권 계좌 + 오픈API 사용 신청 → 앱키·시크릿키 발급 (<https://openapi.kiwoom.com>)
- **미국** — <https://twelvedata.com> 무료 가입 → API Key (15분 지연, 분당 8회 제한)

`server\.env` 를 메모장으로 열어 수정합니다.

```
DEMO=false
KIWOOM_APP_KEY=발급받은_앱키
KIWOOM_SECRET_KEY=발급받은_시크릿키
KIWOOM_MOCK=true          # 모의투자 키면 true, 실전 키면 false

US_PROVIDER=twelvedata
US_API_KEY=발급받은_키
```

**저장은 `Ctrl+S` 로 하세요.** "다른 이름으로 저장"을 쓰면 메모장이 `.env.txt` 로 바꾸는데, 윈도우가 확장자를 숨겨서 `.env` 처럼 보입니다.

수정 후 백엔드 창을 닫고 `start.bat` 을 다시 실행하세요.

### 키 관련 주의

- **다른 사람의 키를 받아 쓰지 마세요.** 그 사람의 API 호출 한도와 계좌 권한을 쓰게 됩니다.
- `server\.env` 는 절대 공유하지 마세요. 키가 그대로 담겨 있습니다.
- 이 프로그램은 **시세 조회만** 합니다. 주문 기능이 없어 매매가 일어나지 않습니다.

기술적인 상세는 `README.md` 를 참고하세요.
